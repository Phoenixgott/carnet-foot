{
  "name": "carnet-paris-foot",
  "version": "0.4.0",
  "description": "Carnet de Paris Foot : application privée et hors ligne d'aide aux méthodes +1.5, +2.5 et Freebet.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "preview": "node scripts/build.mjs && node scripts/serve.mjs dist 4173",
    "build:apercu": "node scripts/build.mjs --apercu",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.sw.json && tsc -p tsconfig.node.json",
    "test": "node --import tsx --test \"tests/unit/**/*.test.ts\"",
    "e2e": "node scripts/build.mjs && node scripts/build.mjs --apercu && playwright test",
    "check": "npm run typecheck && npm test && npm run e2e"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@types/node": "25.6.2",
    "acorn": "8.16.0",
    "esbuild": "0.27.7",
    "playwright": "1.56.0",
    "tsx": "4.21.0",
    "typescript": "6.0.3"
  }
}
